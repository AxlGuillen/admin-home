/**
 * Entry point **de servidor** del módulo: solo lo que importa `server-only`.
 *
 * Server Components y Server Actions usan `@/modules/<nombre>/server`;
 * todo lo demás usa `@/modules/<nombre>`.
 */
export { listExamples } from "./queries";
